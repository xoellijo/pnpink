# This file define global variables, snippets and spritesheets

# =====================
# RICH TEXT SNIPPETS
# =====================
## Bold text:
# :TB(text) = <tspan font-weight='bold'>${text}</tspan>

## Italic text:
# :TI(text) = <tspan font-style='italic'>${text}</tspan>

## Low text (subscript):
# :TL(text) = <tspan baseline-shift='sub' font-size='65%'>${text}</tspan>

## High text (superscript):
# :TH(text) = <tspan baseline-shift='super' font-size='65%'>${text}</tspan>

## Underlined text:
# :TU(text) = <tspan text-decoration='underline'>${text}</tspan>

## Crossed-out text:
# :TX(text) = <tspan text-decoration='line-through'>${text}</tspan>

## Font family:
# :TF(text font size) = <tspan font-family='${font}' ${size? font-size='${size}'}>${text}</tspan>

## Text size:
# :TS(text size) = <tspan ${size? font-size='${size}'}>${text}</tspan>

## Text color and outline:
# :TC(text fill stroke width) = <tspan fill='${fill}' ${stroke? stroke='${stroke}' : stroke='${fill}'} ${width? stroke-width='${width}'} paint-order='stroke'>${text}</tspan>

## Marked text:
# :TM(text styleID border= front=) = <tspan pnp:decoration='#${styleID}' ${border? pnp:decoration-padding='${border}'} ${front? pnp:decoration-layer='${front}'}>${text}</tspan>
# =====================

# =====================
# Other SNIPPETS
# =====================

# snippet for iconify noto icons
# :Ic(icon) = @{icon://noto/${icon}}

# snippet for Tabletop Simulator cache image path (only windows supported)
# :TTS(img) = %USERPROFILE%\Documents\My Games\Tabletop Simulator\Mods\Images\${img}

