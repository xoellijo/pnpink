[0.57]
- Fix rectangle text decorators with user-space fill gradients by normalizing the copied gradient from the style rect's original bbox before fitting it to the measured text.
- Give text decorators 1 pt of default padding on all four sides while preserving explicit border values, including zero.
- Allow headers such as `placeholder=.L{3x1}~i5` to provide a default array Layout together with global Fit/Anchor options; cell-local layouts take precedence.
- Add rich-text decorations through `pnp:decoration` and `:TM(...)`, supporting optional border/layer arguments plus path, layered path-group, and fitted rectangle templates with copied paint styles, corrected round/square caps, front/behind layers, CSS-like padding, and shared persistent-shell measurement.
- Reuse a warm Google Sheets OAuth/HTTPS session for DeckMaker's lifetime, cache sheet titles, remove duplicate range resolution, and show dataset loading separately from SVG rendering.
- Resolve Google Drive folder sources asynchronously across dataset sections, refresh each folder once per generation, and invalidate changed cached files through Drive modification metadata.
- Add dynamic `shape-inside` frames through `textID[shape-inside]` and `Transform{inside=x|y|a}`, including empty-frame removal, inline-icon-aware sizing, variable-height layouts, and reusable gradients.
- Preserve the original flow geometry when fitting transformed `shape-inside` text, so compressed or stretched text cannot disappear after its visible frame is trimmed.
- Add `force_bbox` guide rectangles, with case-insensitive separator variants, to work around unreliable Inkscape bounding boxes for groups containing text.
- Support multiple inline icons sharing one text slot with the same Fit/Anchor/Transform behavior used by multi-value cells.
- Improve snippet parsing so nested braces inside conditional expressions and transforms are handled correctly.
- Speed up text geometry discovery with a persistent Inkscape shell, compact per-card probes, parallel warm-up, and focused profiling.
- Make the persistent-shell pipeline the only text-geometry path, removing the monolithic CLI fallback, experimental backend selector, and unused query-tree minimization code.
- Reuse the text-measurement Inkscape helper across DeckMaker generations, skip text scans when a card has no geometry consumers, and move per-card/per-page diagnostics from INFO to DEBUG.
- Align the DeckMaker application and INX labels with version 0.57 and make release builds reject mismatched application or extension versions.
- Replace the legacy installers with stable `pnpink_install.bat` and `pnpink_install.sh` launchers that download the latest or a selected release; Windows can install a private Ghostscript unless `--no-ghostscript` is used.
- Simplify the Python installer by removing obsolete development-version coexistence and manifest rewriting machinery.
- Rewrite the README and getting-started documentation around approachable card, event-badge, and variable-data PDF workflows, and consolidate plain text, rich text, snippets, inline icons, dynamic frames, and path decorations in a dedicated Text guide.

[0.55]
- Add dataset symbol rows with `symbol_id` in column A, generating a row into `<defs>` without consuming page layout slots.
- Add `Transform{text=...}` / `Transform{t=...}` to replace text inside placed icons or groups; text transforms force real copies instead of shared clones.
- Support `.T{...}` transforms in inline `:id...:` icon tokens.
- Add snippet conditional fallbacks with `${var? true : false}` and update `:Tc` defaults.
- Fix `Transform{text=...}` on styled symbol icons so nested `<tspan>` font/color styles are preserved.
- Add `Transform{scale=...}` / `Transform{s=...}` for centered post-placement scaling; soft edge is now `edge` / `e`.
- Add `inline_icons_extra_ratio` preference to tune inline icon text spacing without resizing the icon; default is `0.00`.
- Add `gdrive://` sources for public Google Drive files and folders, including folder paths, glob filters, selectors, iterators, and local cache.
- Add `gdrive_api_key` preference for Google Drive public folder listing.
- Add `network_workers_gdrive` preference and parallel Google Drive downloads; cache folder listings per run.
- Convert `.webp` bitmap sources to cached `.png` files before placement for more reliable Inkscape/export behavior.
- Support quoted local source paths with spaces.
- Fix deep-copy placement to preserve inherited SVG transforms, avoiding oversized generated symbols from scaled template layers.
- Update Source and Snippets documentation for Google Drive, WebP conversion, and current rich-text snippet definitions.

[0.53]
- Fix Fit/Anchor border sizing after layout scaling, so bleed-like borders keep their final page size.
- Fix layout changes after automatic full-page rows (`?`) to avoid starting the next layout on an already used page.
- Rework `@back` as a back-side control column with mirrored layouts, marks, iterator selectors, and per-card backs.
- Fix back-side sizing so front/back cards share the same final Fit/Anchor behavior.
- Fix iterator selectors such as `[37..?]` and scoped back-side selectors like `[2..? 1]`.
- Fix generated page export selection and first-page geometry updates.
- Fix PDF export auto-rotation by disabling Ghostscript page rotation.
- Improve ZVG/PNP packaging for embedded raster images, asset naming, and PDF-imported document names.
- Add optional AVIF export when the active Pillow runtime supports it.
- Expand documentation for `@back`, iterator selectors, and back-side marks.
